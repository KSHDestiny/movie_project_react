import React from "react";

const NotFoundPage = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        height: "100vh",
        alignItems: "center",
        fontSize: "3rem",
      }}
    >
      <h1>File Not Found</h1>
    </div>
  );
};

export default NotFoundPage;
